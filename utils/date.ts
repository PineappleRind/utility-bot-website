// We can use this time to show the time certain shard's of Utility Bot e.g UTC, EST etc. this is to format time ;)

import {format} from 'date-fns'

const format Date = (date: Date | string) =>
    formate(typeof date ==='string' ? new Date(date) ; Date, 'do MMMM, yyyy')
